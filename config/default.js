const config = {
  // 启动端口
  port: 3000,
  // 数据库配置
  database: {
    DATABASE: 'hxzserver',
    USERNAME: 'root',
    PASSWORD: 'hexuzheng',
    PORT: '3308',
    HOST: 'localhost'
  }
}

module.exports = config